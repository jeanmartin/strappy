= semantic_form_for @user, :url => password_reset_path, :method => :put do |f|
  %h2 Change My Password
  = f.error_messages
  = f.input :password
  = f.submit "Update"

= content_for :on_ready do
  PNB.formFocusFirst('new_user')
